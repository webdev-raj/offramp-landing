export const metadata = {
  title: "Expert Portal | OffRamp",
  description: "Sign in or apply to join the verified network of OffRamp nutrition and metabolic health experts.",
};

export default function ExpertAuthLayout({ children }) {
  return children;
}
